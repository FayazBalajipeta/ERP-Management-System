import React from "react";

const Invoice = () => {
  return (
    <div className="products-container">
      <h2>Invoice</h2>

      <p>This module will generate invoices (PDF coming in Week 4).</p>

      <button>Create Invoice</button>
    </div>
  );
};

export default Invoice;
